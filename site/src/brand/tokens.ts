/**
 * Programmatic equivalents of the canonical values in tokens.css.
 * Metadata and ImageResponse cannot resolve runtime CSS custom properties.
 */
export const serverTokens = {
  obsidian: "rgb(9 9 11)",
  paper: "rgb(244 244 245)",
  mist: "rgb(212 212 216)",
  ember: "rgb(255 90 0)"
} as const;
