# incubator idea index

> Compact incubator meeting context generated from the append-only ledger. The raw JSONL ledger is never injected into a meeting. VAULT compares this namespace with global memory.

No ideas recorded.
