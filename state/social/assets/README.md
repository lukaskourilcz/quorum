# Social asset provenance

Asset metadata lives here. Publishable files live under `site/public/social/`.
Secrets, signed URLs, base64 and external inspiration bytes are forbidden. Every
social visual records a live Carousel Studio template id/version, brand token set,
content payload, PNG hash and `carousel-studio-1` renderer version.
