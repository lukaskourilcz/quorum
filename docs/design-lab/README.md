# Design Lab — the template library

Twenty-three template families for article decks, and the editor workspace the owner drives them
from at `/admin?venture=design-lab&tab=studio`.

This folder started as a delivery an engineering agent was to build from. It is now the other way
round: the engine is built, and these pages are read out of it. `SPEC.md` remains the template
language's binding artifact, but where a family's composer and any description here disagree, the
composer is what ships.

## Reading order

1. **`TOKENS.md`** — the type choices per brand, the weights the templates actually name, the
   spacing scale, the contrast receipts, and the rule that governs where a body passage starts.
2. **`SPEC.md`** — the template language, and the founding ten families in full: thesis, rhythm,
   variant axes, and complete `carousel-template/1` JSON. **This is the binding artifact for the
   language.** For a composition, the composer in `studio/src/` is.
3. **`families/*.html`** — one specimen per family, generated by `pnpm -C studio specimens` from
   the committed composers: every canvas, every palette, and each recipe axis acting on one slide,
   with the checks and the measured type sizes printed underneath. Open them in a browser; they
   are self-contained and make no network requests.

## The families

Ten from the founding delivery, thirteen added on 2026-08-10 from that year's typography-led
design trends. Every one of them rides the same controls: the A/B variant, the photo treatment,
type scale 0.9/1/1.1, the phase seed 0–3, and per-slide text.

| # | Family | Serves | Thesis |
|---|---|---|---|
| 01 | [`masthead`](families/masthead.html) | photo-forward | A front page: the photograph runs full-bleed to a hard rule, and the words live beneath it. |
| 02 | [`gutter`](families/gutter.html) | photo-forward | A hard vertical split whose column changes sides as the reader swipes. |
| 03 | [`bevel`](families/bevel.html) | photo-forward | The photograph is cut on a slope, and the same slope keeps cutting the frame. |
| 04 | [`porthole`](families/porthole.html) | photo-forward | A circular window on the cover, and the same window travelling the deck as an empty ring. |
| 05 | [`slab`](families/slab.html) | type-only | A type poster: the words at poster scale, and the whole slide inverts on the beat. |
| 06 | [`terrace`](families/terrace.html) | type-only | Three offset bands step across the frame and the words sit in the space they leave. |
| 07 | [`figure`](families/figure.html) | type-only | A quantity is the image: the reader's position through the deck, drawn rather than written. |
| 08 | [`pull`](families/pull.html) | photo-forward | A line set at the size it was said with, between an accent bar and a panel that hugs it. |
| 09 | [`tower`](families/tower.html) | photo-forward | A chapter bar climbing the left edge, with the words in a tall column beside it. |
| 10 | [`dossier`](families/dossier.html) | quiet | A record rather than a poster: hairlines, reading-size type and a dashed middle beat. |
| 11 | [`billboard`](families/billboard.html) | type-only | Poster type at the size the words allow: one block filling the band, one rule, nothing else. |
| 12 | [`broadsheet`](families/broadsheet.html) | type-only | An inside page: running head, rule pair, a measured column and an index hanging in its margin. |
| 13 | [`zurich`](families/zurich.html) | type-only | A twelve-column grid worked flush left, where what is left empty does as much as what is set. |
| 14 | [`concrete`](families/concrete.html) | type-only | A hard-edged panel with its own block sitting behind it, and the block changes corners on the beat. |
| 15 | [`terminal`](families/terminal.html) | type-only | Mono from end to end: a bracketed header field, a block cursor opening each passage, dashed rules. |
| 16 | [`marginalia`](families/marginalia.html) | type-only | Clean type with a marker over it: a highlight that hugs the passage, and a hand working the margin. |
| 17 | [`memo`](families/memo.html) | type-only | A typed note on a bordered sheet: reading-weight body type, a short ragged measure, a mono letterhead. |
| 18 | [`versus`](families/versus.html) | type-only | The canvas cut in two at a hard seam, with the loud half and the quiet one trading places on the beat. |
| 19 | [`tally`](families/tally.html) | type-only | An oversized index numeral hung beside the passage, drawn from segments and counting nothing but the deck. |
| 20 | [`counterweight`](families/counterweight.html) | type-only | Ultra-bold against light at one size, with a block in the corner that trades weight with the type. |
| 21 | [`throughline`](families/throughline.html) | type-only | A single line that enters, dips under the passage and leaves at the height the next slide begins on. |
| 22 | [`quiet`](families/quiet.html) | quiet | One short block of type in a great deal of room, a mark the width of a fingernail, nothing else. |
| 23 | [`offset`](families/offset.html) | type-only | The passage printed twice, the accent plate a few thousandths out of true under the foreground one. |

These are the **23 shared article families** named by `DECK_FAMILIES`. Tehdejší svět also has a
dedicated bilingual template kit in `studio/src/families-tehdejsi.ts`: Czech and Ukrainian are
separate required slots on each slide, with Literata/Inter faces whose committed cmap coverage is
tested. It is deliberately not a 24th shared family because the shared family contract has one
passage slot per slide and the bilingual kit must fail if either language is missing.

The current engine inventory is nine brand skins (the original five plus Kvórum,
BOOKSOFHISTORY, Door Money and Tehdejší svět), four canvases (square, portrait, story and
Threads), and one recorded `carousel-recipe/1` per social set. A recipe chooses a family, A/B,
accent swap, photo treatment, type scale and phase seed deterministically, while excluding the
venture's two most recently used families when the pool allows it.

Coverage the brief asked for: photo-forward — masthead, gutter, bevel, porthole, pull.
Type-only, which is to say families that never need a photograph — slab, terrace, figure.
Number-led — figure. Quote-capable — pull, porthole. Hardest in 9:16 — tower. Quiet and
record-keeping — dossier.

## How the specimens are made

`pnpm -C studio specimens [family…]` writes `families/*.html` from the committed composers. Each
canvas on a page is the SVG `renderCarouselSvg` produced, inlined — not a drawing of it. Do not
hand-edit a page; change the composer and re-run.

That is a deliberate change from the founding delivery, whose pages were built from `SPEC.md`'s
JSON through a *port* of the renderer and said in their own text that the engine wins any
disagreement. A port drifts, and this one had: `figure` composes something its spec entry does not
describe. The reviewer's copy was the one document in the folder nobody could trust.

Rendering fails loudly, which is itself part of the evidence: `renderCarouselSvg` refuses a
template whose checks do not pass, so a family that fails cannot have a page at all.

What the pages still do not buy is the typeface. They carry no webfonts, so a browser sets them in
whatever it has; the face intended at each role is named in `TOKENS.md` and the render server has
the real files. Composition, line breaks and type size are honest; the letterforms are not.

## Rules the set holds itself to

- Nothing is invented to fill a frame. A short article renders a short deck; a passage that runs
  long grows down into space the frame already had.
- Every family renders acceptably with no photograph, and each says what replaces it. None of
  the answers is a stock image, and none is a face.
- The photo credit has a frame on every outro, in every family, in every format. A composition
  with nowhere to print it would be invalid, and none of these is.
- No invented statistics, no fake interface elements, no fabricated quotes, no engagement
  ornament. The one family built around a number, Figure, says in its own spec that the slot
  takes an honest figure from the article or the family is not used.
